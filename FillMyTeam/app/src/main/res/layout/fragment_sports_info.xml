<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SportsInfoFragment">

    <!-- TODO: Update blank fragment layout -->
    <!--TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="@string/hello_blank_fragment" />

    <Button android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:id="@+id/button"
        android:text="Insert into DB" /-->



    <android.support.v7.widget.RecyclerView
        android:id="@+id/sports_info_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/list_item_margin"

        android:layout_marginStart="@dimen/side_margin"

        android:layout_marginEnd="@dimen/side_margin"
        android:paddingTop="@dimen/list_item_margin"/>

    <ProgressBar
        android:id="@+id/loading_indicator" android:layout_gravity="center_vertical|center"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/activity_vertical_margin" />

    <TextView
        android:id="@+id/recyclerview_forecast_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_horizontal"
        android:padding="@dimen/activity_vertical_margin"
        android:text="@string/data_unavailable"
        app:layout_anchor="@+id/recyclerview_forecast"
        app:layout_anchorGravity="fill"
        android:textAppearance="?android:textAppearanceSmall"
        android:visibility="invisible"/>

</FrameLayout>
